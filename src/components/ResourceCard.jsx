import React from 'react';
import PropTypes from 'prop-types';

function ResourceCard(props) {
  return (
    <h1>I'm a resource card!</h1>
  );
}

ResourceCard.propTypes = {
};

export default ResourceCard;
